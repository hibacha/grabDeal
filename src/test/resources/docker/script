#start a local docker hub
docker run -d --name hub -P selenium/node-chrome-debug


#start a local docker node of chrome
docker run -d --name chrome-debug --link hub:hub -P selenium/node-chrome-debug
 

 docker ps
 docker-machine ls
 docker-machine create --driver virtualbox default